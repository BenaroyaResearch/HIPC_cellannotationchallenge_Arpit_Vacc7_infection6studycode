# ============================================================================
# nf-core/scdownstream — CELL TYPE ANNOTATION ONLY, NO FILTERING — FULL RUN
# infection_study_06
# ============================================================================
# Run:
#   nextflow run nf-core/scdownstream -r dev \
#     -params-file params_infection6_annotation_full.yaml \
#     -c /home/amishra/.config/nextflow/coder.config
#
# This is the 5k-test params with `sample_n` removed (annotate ALL cells) and a
# separate outdir. If QC_RAW OOMs on the full object, switch to the per-sample
# split samplesheet (prep_infection6.py, MODE="full") — see chat.
# ============================================================================
#input:  './samplesheet_infection6_annotation.csv'
#outdir: './infection6_annotation_full'
input: './samplesheet_infection6_full.csv'
outdir: '/nfs/amishra/Arpit/hipc_challenge/infection6_annotation_full'
species: 'human'
save_intermediates: false
qc_only: true
unify_gene_symbols: false
ambient_correction: 'none'
doublet_detection: ''
cell_cycle_scoring: false
celltypist_model: 'Immune_All_Low,Immune_All_High'
celldex_reference: './celldex_references.csv'
