# ============================================================================
# nf-core/scdownstream — CELL TYPE ANNOTATION ONLY, NO FILTERING — FULL RUN
# infection_study_06
# ============================================================================
# Run (from the hipc_challenge/ working directory):
#   nextflow run nf-core/scdownstream -r dev \
#     -params-file params_infection6_annotation_full.yaml \
#     -c <your-compute-config>.config
#
# If QC_RAW OOMs on the full object, switch to a per-sample split samplesheet.
# ============================================================================

input:  './samplesheet_infection6_full.csv'
outdir: './infection6_annotation_full'
species: 'human'
save_intermediates: false
qc_only: true
unify_gene_symbols: false
ambient_correction: 'none'
doublet_detection: ''
cell_cycle_scoring: false
celltypist_model: 'Immune_All_Low,Immune_All_High'
celldex_reference: './celldex_references.csv'
